frontend:
  phases:
    preBuild:
      commands:
        - cd public
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /public/dist
    files:
      - '**/*'
  cache:
    paths:
      - public/node_modules/**/*
environment:
  variables:
    BACKEND_API_URL: "http://35.182.207.166:3000"
