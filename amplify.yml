frontend:
  phases:
    preBuild:
      commands:
        - npm install # Run npm install from the root directory
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /public/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
  environment:
    variables:
      BACKEND_API_URL: "http://35.182.207.166:3000"
