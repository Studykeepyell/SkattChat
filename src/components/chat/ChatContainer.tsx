import React from 'react';
import { useSocket } from '../../hooks/useSocket';
import '../../styles/chat.css';

const ChatContainer: React.FC = () => {
    // Move your chat.ts logic here
    return (
        <div className="chat-container">
            {/* Your chat UI */}
        </div>
    );
};

export default ChatContainer; 